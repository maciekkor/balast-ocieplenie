{
  "name": "balast-ocieplenie",
  "version": "1.0.0",
  "private": true,
  "description": "Kalkulator balastu i ocieplenia dla nurków (PWA, dane lokalnie)",
  "author": "Maciej Korzeniowski",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.js",
    "serve": "node build.mjs && npx --yes http-server dist -p 8080 -c-1"
  },
  "engines": {
    "node": ">=20"
  }
}
